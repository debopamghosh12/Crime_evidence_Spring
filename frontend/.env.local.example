# Copy to .env.local (gitignored) before running `npm run dev`.
# Must point at the real Spring Boot backend's port (application.yml: server.port, default 8080) -
# never 3000 (Next's own dev server) and never 3001 (a different project's Express backend, not used here).
NEXT_PUBLIC_API_URL=http://localhost:8080
